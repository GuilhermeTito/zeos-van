import { SafeAreaView } from "react-native-safe-area-context"
import { Text } from "react-native"

export default function IndexPassageiro(){
    return (
        <SafeAreaView>
            <Text>Index Motorista</Text>
        </SafeAreaView>
    )
}